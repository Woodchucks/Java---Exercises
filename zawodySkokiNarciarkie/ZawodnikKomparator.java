public class ZawodnikKomparator {
    public boolean compare(Zawodnik zawodnik1, Zawodnik zawodnik2) {
        // Integer.valueOf(zawodnik1.getPunkty()).compareTo(zawodnik2.getPunkty()); //jesli 1 wart wieksza od 2 return da 1 zawodnika
        return false;
    }
}
